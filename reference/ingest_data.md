# Check input data

Used by multiple functions to check the class of input data and to
return the input as a data.table.

## Usage

``` r
ingest_data(data)
```

## Arguments

- data:

  An input data object (data.table, data.frame, matrix)
